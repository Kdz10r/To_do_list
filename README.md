# To_do_list
**ToDoApp** is a lightweight task manager for **macOS** built with **SwiftUI** in **Xcode**. It allows users to add, complete, and delete tasks in a simple interface. Tasks are stored locally in a JSON file, ensuring data persistence between sessions. The project demonstrates basic SwiftUI concepts and clean app structure.
